import React from 'react';

export default React.createClass({
  render: () => {
    return (
      <p>
        Landing page
      </p>
      // username
      // password
      // login button
    );
  }
});
