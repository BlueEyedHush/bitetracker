
import {ensureCookiesEnabled} from 'client/sharedjs/cookies';

describe('test-cookies', () => {
  it('should not leave cookie after test', () => {
    ensureCookiesEnabled(() => {});
    expect(document.cookie).not.to.contain('testcookie');
  })
});
