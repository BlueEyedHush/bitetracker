import config from 'server/config/environment';
import * as app from 'server/app';

export function expressHandler() {
  return app.getApp()
    .then(a => a.getExpressHandler());
}

export function initAppAndPrepareDb() {
  return app.getApp()
    .then(a => a.getDatabase().dropDatabase().then(_ => a));
}
