import mongoose from 'mongoose';

const Schema = mongoose.Schema;

export const mealTypes = ['breakfast', 'lunch', 'dinner'];

export const PlannerEntrySchema = new Schema({
  name: String,
  meal: {
    type: String,
    required: true,
    enum: mealTypes,
  },
  recipe: Schema.Types.ObjectId,
  food: String,
});

export default mongoose.model('PlannerEntry', PlannerEntrySchema);
