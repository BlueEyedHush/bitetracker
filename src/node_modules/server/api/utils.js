
import logger from 'winston';

/**
 *
 * @param response
 * @param error - Error object
 */
export function send400(response, error) {
  logger.warn(`Client error occured: ${error.stack}`);
  response.status(400).json({
    type: error.name,
    message: error.message
  });
}
