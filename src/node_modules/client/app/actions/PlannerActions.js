import assign from 'lodash/assign';
import alt from 'alt-instance';

class GroceryListActions {
  changeDate(date) {
    return date;
  }
}

export default assign(
  alt.generateActions('fetchPlanner'),
  alt.createActions(GroceryListActions),
);
