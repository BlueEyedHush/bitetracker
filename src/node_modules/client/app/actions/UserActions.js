import assign from 'lodash/assign';
import alt from 'alt-instance';

class UserActions {
  setDailyIntake = (intake) => intake;
}

export default assign(
  alt.generateActions('fetchUser'),
  alt.createActions(UserActions)
);
