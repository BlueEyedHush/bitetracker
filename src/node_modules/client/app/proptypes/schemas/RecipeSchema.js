import {PropTypes} from 'react';
import schema from './Schema';

const {string, number, oneOfType} = PropTypes;

const Recipe = {
  _id: oneOfType([string, number]).isRequired,
  title: string.isRequired,
  subtitle: string,
  description: string,
  image: string
};

export default schema(Recipe);
