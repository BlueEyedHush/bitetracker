import {PropTypes} from 'react';
import schema from './Schema';

const MealSchema = {
  _id: PropTypes.string.isRequired,
  name: PropTypes.string.isRequired,
  date: PropTypes.instanceOf(Date).isRequired,
};

export default schema(MealSchema);
