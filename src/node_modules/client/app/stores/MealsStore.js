import alt from 'client/app/alt';

import MealsCrudActions from 'client/app/actions/MealsCrudActions';

class MealsStore {
  constructor() {
    this.date = null;
    this.meals = [];

    this.bindListeners({
      handleFetchMealsSuccess: MealsCrudActions.FETCH_MEALS_SUCCESS
    })
  }

  handleFetchMealsSuccess(mealsList) {
    this.meals = mealsList;
  }
}

export default alt.createStore(MealsStore, 'MealsStore');