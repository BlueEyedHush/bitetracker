import React from 'react'
import { LinkContainer } from 'react-router-bootstrap'

import { Navbar, Nav, NavItem, Glyphicon } from 'react-bootstrap'

import Omnibox from './Omnibox.jsx'

import './navbar.scss'

export default class MainNavbar extends React.Component {
  constructor(props) {
    super(props);
  }

  _onSearch(searchTerm) {
    console.log(searchTerm);
  }

  render() {
    return (
      <Navbar>
        <Navbar.Header>
          <Navbar.Brand>
            <a href="#">BiteTracker</a>
          </Navbar.Brand>
        </Navbar.Header>

        <Navbar.Form pullLeft>
          <Omnibox onSearch={this._onSearch} />
        </Navbar.Form>

        <Nav pullRight>
          <LinkContainer to="/recipes">
            <NavItem eventKey={1}>Recipes</NavItem>
          </LinkContainer>
          <LinkContainer to="/log">
            <NavItem eventKey={2}>Food Log</NavItem>
          </LinkContainer>
          <LinkContainer to="/planner">
            <NavItem eventKey={3}>Meal Planner</NavItem>
          </LinkContainer>
          <LinkContainer to="/groceries">
            <NavItem eventKey={4}>Groceries</NavItem>
          </LinkContainer>

          <NavItem eventKey={5} href="#" className="nav-icon">
            <Glyphicon glyph="bell" aria-label="notifications" />
          </NavItem>

          <LinkContainer to="/profile">
            <NavItem eventKey={6} className="nav-icon">
              <Glyphicon glyph="user" aria-label="profile" />
            </NavItem>
          </LinkContainer>
          <LinkContainer to="/settings">
            <NavItem eventKey={7} className="nav-icon">
              <Glyphicon glyph="cog" aria-label="settings" />
            </NavItem>
          </LinkContainer>
        </Nav>
      </Navbar>
    );
  }
}
